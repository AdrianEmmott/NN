import { RemoveSlashesAndDashesDirective } from './remove-slashes-and-dashes.directive';

describe('RemoveSlashesAndDashesDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSlashesAndDashesDirective();
    expect(directive).toBeTruthy();
  });
});
